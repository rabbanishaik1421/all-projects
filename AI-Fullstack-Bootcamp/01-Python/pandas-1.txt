import pandas as pd

# series

var1 = [12,34,56,79,57]

marks = pd.Series(var1)

print(marks)
print(type(marks))

print(marks[1:4])

sensor_data = {
    "sensor1" : list(range(1,11)),
    "sensor2" : [x**2 for x in range(1,11)],
    "sensor3" : [x**3 for x in range(1,11)],
    "sensor4" : ["entry_"+str(x) for x in range(1,11)]
}

df = pd.DataFrame(sensor_data)

print(type(df))
df

# access a column
df["sensor4"]

# access the row

df.iloc[6]

# access the entry

df.iloc[6]["sensor3"]

df["sensor3"][6]

df.info()

df.describe()

df["sensor1"].mean()

df[["sensor3","sensor1"]]

df.iloc[[4,3,8]]


df.iloc[[4,3,8]][["sensor3","sensor1"]]

df[3:8:2]

import kagglehub

# Download latest version
path = kagglehub.dataset_download("jayapriyadatascience/wineqt-dataset")

print("Path to dataset files:", path)

import os
file_path = os.path.join(path,"WineQT.csv")
print(file_path)

wine_df = pd.read_csv(file_path)
wine_df.info()

wine_df

wine_df.head()

wine_df.tail()

# We want : what is the chloride value when the fixed acidity is least
wine_df["fixed acidity"].min()

wine_df[wine_df["fixed acidity"] == wine_df["fixed acidity"].min()]["chlorides"]

What is a DataFrame?
What is a Series?
What is shape?
What does dtypes return?
What is value_counts()?
How do you read a CSV file?